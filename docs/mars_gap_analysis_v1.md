# MARS-FIELD vs Original Mars Program Gap Analysis

| Original design goal | Current implementation state | Missing work to close the gap |
| --- | --- | --- |
| `Cld` as the main functional enzyme line for Mars-relevant stress resilience | `Cld` is implemented as both benchmark target and topic-aware scoring case; ASR and structure mapping exist in the broader workspace | integrate `Cld` wet-lab readouts and stress proxy results back into the platform objectives |
| `AresG` as a protection-module line for reversible protective microenvironments | `AresG` exists as a topic-specific route and scoring concept, but not as a first-class platform workflow | connect `AresG` outputs to platform-native analysis, reporting, and future design objectives |
| `DrwH` as a cargo-cap / compact protection-domain line | `DrwH` is represented in project docs and topic logic, but not as a complete platform-native benchmark path | add direct platform-facing inputs, outputs, and protection-specific evaluation loops |
| Mars-relevant multi-stress functional resilience as the biological story | Current platform is a generalized evidence-to-sequence controller with benchmark-first validation | reintegrate Mars-specific readouts such as ROS challenge, low-temperature activity, freeze-thaw, dry-rehydration, and perchlorate-linked assays |
| A unified workflow that can move from concept to reusable engineering system | Current code already implements a unified field-style prototype with benchmark, decoder, calibration, and paper assets | turn the platform into a more turnkey raw-PDB and cross-machine release experience |
